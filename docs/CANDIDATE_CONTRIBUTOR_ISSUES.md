# Candidate contributor issues

These are truthful future work candidates, not claims that contributors/users already exist.

1. Add a signed-receipt provider protocol with an Ed25519 reference implementation and algorithm-agility tests.
2. Add an optional OpenTelemetry event sink without introducing telemetry into core runtime dependencies.
3. Build a SQLite persistence adapter with crash/replay tests and explicit transaction semantics.
4. Add maintained Google ADK compatibility tests around the dependency-free callback adapter.
5. Publish cross-runtime EBCJ/conformance fixtures and investigate RFC 8785/JCS interoperability trade-offs.
